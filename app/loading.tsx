import { Loader } from "@/components/layout/Loader";
import React from "react";

const Loading = () => {
  return <Loader />;
};

export default Loading;

// error.tsx 와 Page.tsx, layout.tsx처럼 기본적으로 Next.js에서 서비스하는 파일이다. 존재하면 알아서 렌더링된다.
